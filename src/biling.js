import React, { Fragment } from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
    <React.StrictMode>
    <Fragment>
        <h1>BILLING PAGE</h1>
    </Fragment>
    </React.StrictMode>,
    document.getElementById("billing")
);
